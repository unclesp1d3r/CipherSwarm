---
ignore:
  # CVE-2023-38337 (CVSS 7.5): directory traversal in rswag-api allows reading
  # arbitrary JSON/YAML files. Fix shipped in rswag 2.10.1.
  #
  # False positive: rswag master (reported as 0.0.0) post-dates the fix.
  # Safeguard: rswag-api is NOT mounted in production routes — only available
  # in development/test (see config/routes/admin.rb).
  #
  # Remove this ignore when rswag publishes a proper released gem (see #646).
  - CVE-2023-38337
