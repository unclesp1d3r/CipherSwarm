<ul class="navbar-nav <%= @extra_classes.presence %>">
  <li class="nav-item dropdown">
    <button type="button" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
      <% if @icon.present? %>
        <%= helpers.icon(@icon) %>
      <% end %>
      <%= @title %>
    </button>
    <ul class="dropdown-menu">
      <%= content %>
    </ul>
  </li>
</ul>
