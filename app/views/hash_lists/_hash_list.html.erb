<tr id="<%= dom_id hash_list %>">
  <%= turbo_stream_from hash_list %>
  <td><%= hash_list.name %></td>
  <td><%= truncate hash_list.description %></td>
  <td><%= hash_list.hash_type.name %></td>
  <td><%= hash_list.project.name %></td>
  <td><%= hash_list.completion %></td>
  <td>
    <div class="btn-group btn-group-sm" role="group" aria-label="<%= hash_list.name %> actions">
      <%= link_to icon("eye"), hash_list, class: "btn btn-primary btn-sm", aria: { label: "View #{hash_list.name}" } if can? :read, hash_list %>
      <%= link_to icon("pencil"), edit_hash_list_path(hash_list), class: "btn btn-warning btn-sm", aria: { label: "Edit #{hash_list.name}" } if can? :update, hash_list %>
      <%= link_to icon("trash"), hash_list, method: :delete,
                                            data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                                            class: "btn btn-danger btn-sm", aria: { label: "Delete #{hash_list.name}" } if can? :destroy, hash_list %>
    </div>
  </td>
</tr>
