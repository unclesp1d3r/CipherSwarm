# Residual Review Findings — Issue #568 (Broadcast Throttling)

**Branch:** `568-perf-implement-broadcast-throttlingdebouncing-for-high-throughput-scenarios` **Review run:** `/tmp/compound-engineering/ce-code-review/20260514-212512-1524616c/` **Reviewers:** ce-correctness, ce-testing, ce-maintainability, ce-project-standards, ce-agent-native, ce-learnings-researcher, ce-performance, ce-reliability, ce-kieran-rails **Date:** 2026-05-14

This file captures the unresolved actionable findings from the autofix code-review pass for [#568](https://github.com/unclesp1d3r/CipherSwarm/issues/568). Items applied automatically are already in the branch (`fix(review):` commit). Items below either change behavior beyond autofix scope, are pre-existing issues outside this PR's scope, or are operational/documentation follow-ups.

## Residual Review Findings

- **[P1][gated_auto→downstream-resolver]** \[`app/models/concerns/safe_broadcasting.rb` / `app/models/attack.rb` / `app/models/campaign.rb`\] — **Add namespace prefix to throttle keys** (kieran-rails, confidence 75). Throttle keys `attack_progress_<id>` and `campaign_eta_<id>` share Redis keyspace with the rest of `Rails.cache`. The defensive convention (used by `HashItem`: `broadcast_recent_cracks:<hash_list_id>:<campaign_id>`) would scope to `throttle:broadcast:<concept>_<id>`. Behavior-changing fix — existing in-flight keys would be invalidated; should be a deliberate follow-up.
- **[P1][manual→downstream-resolver]** \[`config/environments/production.rb:61`\] — **Production Redis cache uses `pool: false`** (reliability, confidence 85). With 10+ concurrent agents each firing Task transitions, the throttle's per-transition cache writes serialize through a single Redis connection. Throttle reduces *broadcast* volume but adds connection contention. Pre-existing infra choice; address as a Redis-pool refactor PR alongside operational monitoring.
- **[P2][manual→downstream-resolver]** \[`app/models/concerns/attack_state_machine.rb:114` + `app/models/attack.rb:224`\] — **Pre-existing double-trigger** (performance, confidence 100). Both `AttackStateMachine#after_transition` and `Attack#after_commit on: [:update]` invoke `broadcast_attack_progress_update`. The new throttle absorbs the redundant broadcast, but two Redis `SET NX EX` commands still fire per state transition. Fix requires deciding which callback owns the broadcast — `after_commit` also covers non-state-change updates (e.g., `attacks_count` touch), so a guard like `if: :saved_change_to_state?` would silently drop those.
- **[P2][manual→downstream-resolver]** \[`app/models/concerns/attack_state_machine.rb:114`\] — **Pre-existing: missing `safe_broadcast_*` wrapper in AttackStateMachine** (reliability, confidence 70). `TaskStateMachine` calls `safe_broadcast_attack_progress_update` (with its own `rescue StandardError`), but `AttackStateMachine#after_transition` calls `broadcast_attack_progress_update` directly. Risk is bounded because the state-machine path is less burst-prone than the Task path. Defer for a separate hardening PR.
- **[P2][manual→downstream-resolver]** \[`GOTCHAS.md`\] — **Document Sidekiq enqueue failure → 5s UI blackout** (reliability, confidence 75). If `broadcast_replace_later_to` raises after the throttle key was already set, the throttle absorbs the next 5s of attempts and no broadcast reaches the UI. Operational doc note; pairs with metric/alerting on suppression rate.
- **[P3][manual→downstream-resolver]** \[`GOTCHAS.md` / `config/environments/development.rb`\] — **Document dev-vs-prod throttle behavior divergence** (reliability, confidence 80). Development with no `REDIS_URL` uses `:null_store`, where every `write` returns `true` regardless of `unless_exist:` — meaning throttling is silently disabled in dev. Could also ship a startup warning, but that requires deciding on a config strategy first.
- **[P3][advisory→human]** \[`docs/solutions/best-practices/broadcast-throttling.md` (new)\] — **Capture the pattern in docs/solutions/** (learnings-researcher, confidence 100). No prior `docs/solutions/` entry exists for broadcast throttling; once the pattern proves out in production, write up the leading-edge vs trailing-edge decision so future broadcasters reuse it.

## Coverage Notes

- All applied autofixes are in the `fix(review):` commit on this branch.
- ci-check passes (1960 examples, 0 failures, undercover green) before and after autofix.
- Suppressed-below-gate findings (extracted helper for throttle keys, `.once` vs `.at_least(:once)`, `hash_including` consistency tweak) are recorded in the synthesis artifact at `/tmp/compound-engineering/ce-code-review/20260514-212512-1524616c/synthesis.json`.
