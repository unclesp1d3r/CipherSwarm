# SvelteKit Environment Variables Example

# --- Backend API Configuration ---
# Base URL for the CipherSwarm backend API (for server-side requests)
API_BASE_URL=http://localhost:8000

# Public API base URL (accessible from browser in client-side requests)
# Note: This will be exposed to the client, use only for public endpoints
PUBLIC_API_BASE_URL=http://localhost:8000

# --- SvelteKit Configuration ---
# Node.js environment (development, production, test)
NODE_ENV=development

# Origin URL for the frontend application
ORIGIN=http://localhost:5173

# --- Security ---
# Session secret for server-side session management (if using cookies)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# --- Development ---
# Enable detailed error reporting in development
VITE_DEV_MODE=true

# Enable verbose logging
VITE_LOG_LEVEL=debug

# --- Feature Flags ---
# Enable/disable specific features during development
VITE_ENABLE_LIVE_UPDATES=true
VITE_ENABLE_BENCHMARKS=true

# --- Build Configuration ---
# Build target (used by Vite for optimization)
VITE_BUILD_TARGET=esnext
