<!-- Stub summary for attack validation (to be replaced in Phase 3) -->
<div class="p-4 bg-green-50 border border-green-200 rounded">
    <h4 class="font-bold text-green-700">Attack Validated</h4>
    <div class="mt-2">
        <p><strong>Mode:</strong> {{ attack.mode }}</p>
        <p><strong>Keyspace:</strong> {{ keyspace }}</p>
        <p><strong>Complexity:</strong> {{ complexity }}</p>
        <p><strong>Complexity Score:</strong> {{ complexity_score }}</p>
    </div>
</div>